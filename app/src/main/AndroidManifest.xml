<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      The <queries> block is required on Android 11+ for `resolveActivity` to
      see other apps. Nexus needs to know whether Gmail / Calendar / Contacts
      / Phone / Camera are installed, so the Assistive Mode tutorials can
      offer the "Open …" deep-link button.
    -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="vnd.android.cursor.dir/contact" />
        </intent>
        <package android:name="com.google.android.gm" />
        <package android:name="com.google.android.calendar" />
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:name=".NexusApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Nexus"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:targetApi="33">

        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <!--
          Place 1 of 4 — assistive-purpose declaration (see GOOGLE_COMPLIANCE.md).
          Nexus is an accessibility tool that teaches first-time smartphone users
          to operate the apps already on their phone (Gmail, Calendar, Contacts,
          Camera). When an OAuth scope is granted by the user, Nexus only acts
          on that user's behalf, with explicit per-action confirmation, and
          tokens never leave the device.
        -->
        <meta-data
            android:name="com.nexus.app.PURPOSE"
            android:value="assistive_technology_for_first_time_smartphone_users" />
        <meta-data
            android:name="com.nexus.app.SENSITIVE_SCOPES_USAGE"
            android:value="user_owned_data_only_with_explicit_per_action_confirmation" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Nexus.Splash"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|uiMode|locale|layoutDirection|fontScale|density"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth redirect activity (Google OAuth callback) -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${appAuthRedirectScheme}" />
            </intent-filter>
        </activity>
    </application>

</manifest>
